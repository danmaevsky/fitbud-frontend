export default function WorkoutSearchPage() {
    return (
        <div>
            <h4>This page is still being built! Come back at a later time to see it when it's finished.</h4>
        </div>
    );
}
